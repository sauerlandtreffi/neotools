---
import Base from '../../../layouts/Base.astro';
import ToolGrid from '../../../components/ToolGrid.astro';
import { t } from '../../../lib/i18n';
const locale = 'de' as const;
---
<Base locale={locale} title="Alle Werkzeuge" description="Alle Werkzeuge nach Kategorie – Beschreibung, Optionen, Formate." path="/tools" wide>
  <h1>{t(locale, 'tools')}</h1>
  <div class="mt-8"><ToolGrid locale={locale} /></div>
</Base>
