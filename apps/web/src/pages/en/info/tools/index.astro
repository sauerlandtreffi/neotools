---
import Base from '../../../../layouts/Base.astro';
import ToolGrid from '../../../../components/ToolGrid.astro';
import { t } from '../../../../lib/i18n';
const locale = 'en' as const;
---
<Base locale={locale} title="All tools" description="Every tool by category – description, options, formats." path="/tools" wide>
  <h1>{t(locale, 'tools')}</h1>
  <div class="mt-8"><ToolGrid locale={locale} /></div>
</Base>
